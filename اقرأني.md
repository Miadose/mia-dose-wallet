# Mia Dose — بطاقة Apple Wallet

هذا المشروع يولّد بطاقة Apple Wallet (pkpass) لكل عميل فيها اسمه وباركود ثابت.
راح تسوي 3 مراحل بس: (1) تجيب شهادة آبل، (2) تنشر السيرفر، (3) تربطه بـ FlutterFlow.

---

## المرحلة 1: جيب شهادة Apple Wallet (مرة وحدة بس)

لازم يكون عندك حساب Apple Developer (مدفوع، $99/سنة — قلت لي عندك أصلاً ✅).

### أ) سجّل Pass Type ID
1. روح لـ https://developer.apple.com/account/resources/identifiers/list
2. اضغط + (زر إضافة جديد) → اختر **Pass Type IDs** → Continue
3. حط وصف زي `Mia Dose Loyalty Card` والـ Identifier زي:
   `pass.com.miadose.loyalty`
   (لازم يبدأ بـ `pass.` وتقدر تغير `com.miadose` لاسم نطاقك أو حزمة تطبيقك)
4. سجّله (Register)

### ب) ولّد شهادة (Certificate) لهذا الـ Pass Type ID
1. من نفس الصفحة، افتح الـ Pass Type ID اللي سويته
2. اضغط **Create Certificate**
3. راح يطلب منك ملف CSR (Certificate Signing Request) — تسويه من جهاز Mac عن طريق:
   - افتح **Keychain Access** (تطبيق موجود بكل ماك)
   - من القائمة: Keychain Access → Certificate Assistant → Request a Certificate From a Certificate Authority
   - حط إيميلك، واختر "Saved to disk"
   - راح يطلع لك ملف `.certSigningRequest`
4. ارفع هذا الملف بموقع آبل، وحمّل الشهادة الناتجة (`.cer`)
5. دبل كلك على ملف `.cer` عشان يتحمل في Keychain Access عندك بالماك

### ج) صدّر الشهادة كملفات .pem (تحتاجها للسيرفر)
من نفس جهاز الماك، افتح Terminal وسوي:

```bash
# صدّر الشهادة + المفتاح الخاص كملف .p12 من Keychain Access أولاً
# (كلك يمين على الشهادة بالـ Keychain → Export → احفظها باسم Certificates.p12 وحط باسورد)

# بعدين حولها لـ .pem:
openssl pkcs12 -in Certificates.p12 -clcerts -nokeys -out signerCert.pem -legacy
openssl pkcs12 -in Certificates.p12 -nocerts -out signerKey.pem -legacy
```

راح يطلب منك الباسورد اللي حطيته وقت التصدير — احفظه، بتحتاجه بملف `.env`.

### د) حمّل شهادة Apple WWDR
من: https://www.apple.com/certificateauthority/
حمّل **Apple Worldwide Developer Relations Certification Authority** (G4 عادة أحدث نسخة)، وحوّلها:

```bash
openssl x509 -inform der -in AppleWWDRCAG4.cer -out wwdr.pem
```

### هـ) حط الملفات الثلاثة بمكانها
انسخ `wwdr.pem` و `signerCert.pem` و `signerKey.pem` داخل مجلد `certs/` بالمشروع.

### و) عدّل ملف `.env`
انسخ `.env.example` وسمّه `.env`، وحط فيه الباسورد اللي استخدمته وقت تصدير الشهادة:
```
CERT_PASSPHRASE=الباسورد_حقك
```

### ز) عدّل ملف `mia-dose.pass/pass.json`
غيّر:
- `passTypeIdentifier` → لنفس القيمة اللي سجلتها بالخطوة (أ)، مثل `pass.com.miadose.loyalty`
- `teamIdentifier` → تلقاها بأعلى يمين صفحة https://developer.apple.com/account (Team ID، 10 أحرف/أرقام)

---

## المرحلة 2: انشر السيرفر (استضافة مجانية)

أسهل خيار مجاني: **Render.com**

1. سوّي حساب على https://render.com (فيه خطة مجانية Free tier)
2. ارفع مجلد المشروع كامل (بدون `node_modules`) على GitHub (repo خاص)
3. من Render: New → Web Service → اربط الـ repo
4. Build Command: `npm install`
5. Start Command: `npm start`
6. أضف Environment Variable: `CERT_PASSPHRASE` بنفس القيمة اللي بملف `.env`
7. Deploy — بعد ما يخلص راح يعطيك رابط زي:
   `https://mia-dose-wallet.onrender.com`

### تأكد إنه شغال
افتح بالمتصفح:
```
https://mia-dose-wallet.onrender.com/generate-pass?name=Ahmad&id=12345
```
إذا نزّل لك ملف `.pkpass` (أو فتح Wallet لو من آيفون) → تمام، يشتغل ✅

---

## المرحلة 3: اربطه بـ FlutterFlow

1. من FlutterFlow: Settings → API Calls → **Add API Call**
2. Method: `GET`
3. URL:
   `https://mia-dose-wallet.onrender.com/generate-pass`
4. Query Parameters:
   - `name` = (متغير: اسم المستخدم الحالي، من بيانات حسابه)
   - `id` = (متغير: user ID أو رقم العضوية حقه)
5. على زر "أضف إلى Wallet" بالتطبيق:
   - Action: **API Call** (اللي سويته)
   - بعدها: خذ الرابط الكامل (base URL + query) ونفّذ عليه **Launch URL**
     (على آيفون، فتح رابط بصيغة pkpass يفتح تلقائياً شاشة "Add to Apple Wallet")

---

## ملاحظات مهمة
- التصميم الحالي (اللوقو والألوان) placeholder بسيط بالتيل — بدّل ملفات `icon.png` و `logo.png` داخل `mia-dose.pass/` بلوقو ميّا دوز الحقيقي (نفس الأبعاد: icon 29×29 / 58×58 / 87×87، logo 160×50 / 320×100).
- هذي بطاقة **ثابتة** (اسم + باركود بس) — ما تتحدث لايف بالنقاط. لو احتجت تحديث لايف بعدين نضيف Web Service للـ push updates.
- Render المجاني "ينام" إذا ما فيه طلبات لفترة، أول طلب بعد فترة راحة ياخذ كم ثانية إضافية — طبيعي بالخطة المجانية.
